server:
  port: 8090
spring:
  application:
    name: payment

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8 #设置时区

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://10.9.1.71:3306/payment_demo?serverTimezone=GMT%2B8&characterEncoding=utf-8
    username: wlxy
    password: +Fsa22856762Tla

mybatis-plus:
  configuration: #sql日志
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  mapper-locations: classpath:com/xjw/payment/mapper/xml/*.xml

logging:
  level:
    root: info
springfox:
  documentation:
    swagger-ui:
      enabled: false #开发环境设置为false,正式环境时改成true
